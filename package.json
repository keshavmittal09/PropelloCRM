{
  "name": "propello-crm-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
